{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd cot-backend && npm install && npm run build && cd ../cot-frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd cot-backend && node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
